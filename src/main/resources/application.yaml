spring:
  profiles:
    active: development
---
spring:
  profiles: development
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${database.url}
    username: ${database.user}
    password: ${database.password}
kerberos:
  ad-server: ldap://s-dc01.rosseti.ural:389
  service-principal: HTTP/10.81.5.100@rosseti.ural
  keytab-location: keytab/water.keytab
  ldap-search-base: dc=example,dc=org
  ldap-search-filter: "(| (userPrincipalName={0}) (sAMAccountName={0}))"
server:
  port: 8081
---
spring:
  profiles: production
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${database.url}
    username: ${database.user}
    password: ${database.password}
server:
  port: 8082